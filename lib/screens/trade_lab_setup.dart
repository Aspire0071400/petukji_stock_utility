import 'package:flutter/material.dart';

class TradeLabSetup extends StatelessWidget {
  const TradeLabSetup({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
